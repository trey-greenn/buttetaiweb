/** @type {import('next-sitemap').IConfig} */
const config = {
    siteUrl: 'https://buffetai.co', // Replace with your site's URL
    generateRobotsTxt: true, // (optional) Generate robots.txt file
    // Other options can be added here
  };
  
  module.exports = config;